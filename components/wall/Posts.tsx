export default function Posts(posts: any) {
    return (
      <div>
        {/* Render the posts */}

        {
        /*
            posts.map((post) => (
            <div key={post.id}>
                
            </div>
            )) 
        */
        }
        
      </div>
    );
  }